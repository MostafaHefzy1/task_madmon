enum ToggleStates {
  isStarted,
  isSelected,
}

enum ReservedStates {
  isInitial,
  reserved,
  notReserved,
}

enum DateStates {
  isInitial,
  newest,
  oldest,
}

enum LocationStates {
  isInitial,
  cairo,
  nasrCity,
}

enum ItemReservationStates {
  newState,
  contractSigned,
  noAnswer,
}
